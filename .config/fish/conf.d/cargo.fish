
# This is typically done by setting it once as a universal var instead of on startup, but that
# means having to remember to do it on any new machine. This is easy to sync.
set fish_user_paths $HOME/.cargo/bin $fish_user_paths
